<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
            android:id="@+id/action_attribute_list"
            android:icon="?attr/viewAsListIcon"
            android:title="@string/action_node_list"
            android:showAsAction="never"
            app:showAsAction="never"/>
    <item
            android:id="@+id/action_next_attribute"
            android:icon="?attr/nextIcon"
            android:title="@string/action_next_attribute"
            android:onClick="nextAttribute"
            android:showAsAction="ifRoom"
            app:showAsAction="ifRoom"/>
</menu>
