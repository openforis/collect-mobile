<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        app:showAsAction="always"
        android:title="More"
        android:icon="?attr/moreIcon"
        android:orderInCategory="1000"> <!-- always shown at the end -->
        <menu>
            <item
                android:id="@+id/action_backup"
                android:title="@string/action_backup"
                android:icon="?attr/backupIcon"
                app:showAsAction="never"
                android:onClick="backup"/>
            <item
                android:id="@+id/action_export"
                android:title="@string/action_export"
                android:icon="?attr/exportIcon"
                app:showAsAction="never"
                android:onClick="exportDialog"/>
            <item
                android:id="@+id/action_send_to_collect"
                android:title="@string/action_submit_data_to_collect"
                android:icon="?attr/submitToCollectIcon"
                app:showAsAction="never"
                android:onClick="navigateToSendDataToCollect"/>
            <item
                android:id="@+id/action_survey_list"
                android:title="@string/action_survey_list"
                android:icon="?attr/surveyListIcon"
                app:showAsAction="never"
                android:onClick="navigateToSurveyList"/>
            <item
                android:id="@+id/action_settings"
                android:title="@string/action_settings"
                android:icon="?attr/settingsIcon"
                app:showAsAction="never" />
        </menu>
    </item>

</menu>
